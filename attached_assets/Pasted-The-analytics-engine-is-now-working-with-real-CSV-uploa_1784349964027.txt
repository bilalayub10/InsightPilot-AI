The analytics engine is now working with real CSV uploads.

I want to transform InsightPilot AI into an intelligent business analytics platform.

Do NOT modify the frontend design.

Do NOT change the existing API structure.

Only extend the backend.

Implement the following modular services inside backend/services:

1. business_classifier.py

Create a BusinessClassifier class.

It should automatically detect the business domain from dataset column names.

Support at least:

- Sales
- Marketing
- HR
- Finance
- Inventory
- Customer Support
- Operations
- Healthcare
- Education
- Generic

Use robust keyword matching with synonyms.

Return:

{
  "domain": "...",
  "confidence": 0-100,
  "matched_columns": [...]
}

------------------------------------------------

2. kpi_detector.py

Create a KPIDetector class.

Based on the detected business domain and dataframe columns,
automatically detect the most meaningful KPIs.

Examples:

Sales:
Revenue
Profit
Orders
Average Order Value

Marketing:
Spend
CTR
Conversions
Clicks

HR:
Employees
Average Salary
Attrition

Finance:
Revenue
Expenses
Net Income

Inventory:
Stock
Inventory Value
Low Stock Items

Return KPIs in a generic structure:

[
 {
   "label":"Revenue",
   "value":123456,
   "description":"..."
 }
]

Do NOT hardcode values.

Calculate them from the dataframe whenever possible.

------------------------------------------------

3. chart_planner.py

Create a ChartPlanner class.

Based on dataframe columns and detected business domain,
recommend appropriate charts.

Examples:

Time column
→ Line Chart

Category + Numeric
→ Bar Chart

Many numeric columns
→ Histogram

Categorical distribution
→ Pie Chart

Return:

[
 {
   "type":"line",
   "x":"Order Date",
   "y":"Sales",
   "title":"Sales Trend"
 }
]

------------------------------------------------

4. anomaly_detector.py

Create an AnomalyDetector.

Use simple statistical methods
(IQR or Z-score)

Return:

- unusual values
- missing data warnings
- duplicate warnings
- suspicious distributions

------------------------------------------------

5. Update analyze.py

After profiling the dataframe:

Run:

AnalyticsService

↓

BusinessClassifier

↓

KPIDetector

↓

ChartPlanner

↓

AnomalyDetector

Return everything in a single JSON response.

Do NOT implement Gemini yet.

Do NOT generate executive summaries yet.

Keep the backend modular.

Every service must have one responsibility.

The application must still run successfully after all changes.

Add comments and keep the code production quality.