The ChartPlanner has been completely redesigned to be domain-aware, but the frontend still displays the same charts (Revenue Trend and Channel Distribution) regardless of the uploaded dataset.

Perform a complete end-to-end debugging of the chart generation pipeline.

Trace the entire flow:

CSV Upload
→ BusinessClassifier
→ KPIDetector
→ ChartPlanner
→ analyze.py
→ API Response
→ Frontend Dashboard

For every stage, verify the actual data being passed.

Specifically:

1. Add temporary logging to ChartPlanner.plan() to print:
   - detected domain
   - numeric columns
   - categorical columns
   - temporal columns
   - final chart list returned

2. Verify analyze.py actually uses the return value from ChartPlanner.plan().

3. Verify analyze.py does NOT overwrite charts with any hardcoded defaults.

4. Verify no service modifies chart recommendations after ChartPlanner returns them.

5. Verify the API response contains the exact chart list returned by ChartPlanner.

6. Inspect the frontend.

Determine whether:
- it renders charts dynamically from the API response,
- or if it contains hardcoded chart cards,
- or slices the array incorrectly,
- or maps every chart to Revenue Trend and Channel Distribution.

7. Remove any hardcoded fallback chart definitions.

8. Ensure every uploaded dataset generates charts based solely on:
   - detected business domain
   - detected columns
   - available numeric/categorical/time fields

9. If ChartPlanner cannot produce a domain-specific chart, automatically fall back to generic heuristics.

10. Never fabricate charts.

11. Return the exact chart specifications generated by ChartPlanner.

12. Ensure charts update immediately after every new dataset upload.

Also improve ChartPlanner itself:

• Add semantic scoring instead of first-match keyword selection.
• Ignore ID columns (CustomerID, OrderID, EmployeeID, InvoiceID, etc.).
• Ignore high-cardinality categorical columns (>30 unique values).
• Rank charts by business value.
• Avoid semantically duplicate charts.
• Return richer metadata for each chart:

{
  "type": "...",
  "x": "...",
  "y": "...",
  "title": "...",
  "priority": 95,
  "confidence": 0.92,
  "reason": "...",
  "business_question": "...",
  "aggregation": "sum"
}

Maintain the existing architecture.

Do not rewrite unrelated services.

Fix the root cause rather than adding temporary workarounds.

At the end, provide a summary explaining exactly where the charts were getting stuck and what was changed.