You are a senior Staff Python Engineer and AI Systems Architect.

Your task is to refactor the existing InsightPilot AI backend by replacing the entire rule-based BusinessContextBuilder with an LLM-powered implementation using the OpenAI Responses API.

This is not a prototype.

Write production-quality code that is clean, modular, typed, documented, and easy to maintain.

Goal

The analytics pipeline already computes factual analytical results.

DO NOT change any of these services.

Keep them exactly as they are.

AnalyticsService
BusinessClassifier
KPIDetector
ChartPlanner
AnomalyDetector

These services calculate facts.

The LLM should ONLY convert those facts into business intelligence.

Existing Pipeline

Current pipeline

CSV
↓

AnalyticsService
↓

BusinessClassifier
↓

KPIDetector
↓

ChartPlanner
↓

AnomalyDetector
↓

BusinessContextBuilder
↓

AnalyzeResult

Replace only the BusinessContextBuilder stage.

The new pipeline should become

CSV
↓

AnalyticsService
↓

BusinessClassifier
↓

KPIDetector
↓

ChartPlanner
↓

AnomalyDetector
↓

LLMBusinessContext
↓

AnalyzeResult

Requirements

Create a new service

services/llm_business_context.py

Create a class

LLMBusinessContext

with an async method

build(
profile,
classification,
kpis,
anomalies,
chart_plan
)

The method should return the exact BusinessContext structure currently expected by the frontend.

Do not change the frontend.

Do not change the API schema.

Do not change AnalyzeResult.

OpenAI

Use the OpenAI Responses API.

Use the newest supported SDK.

Read the API key from environment variables.

Do not hardcode secrets.

Prompt Engineering

The system prompt must instruct the model that it is an experienced Business Intelligence Consultant.

It must strictly follow these rules.




Never invent statistics.




Never fabricate KPIs.




Never fabricate trends.




Never fabricate anomalies.




Every statement must be supported by the supplied JSON.




If information is unavailable, explicitly state that there is insufficient evidence.




Never mention OpenAI.




Never mention AI.




Never mention LLM.




Write like a senior McKinsey or Bain consultant.




Use concise executive language.




Return VALID JSON ONLY.

No markdown.

No explanations.

No prose outside JSON.

User Payload

Send the model ONLY structured factual data.

Like

{
"profile": ...,
"classification": ...,
"kpis": ...,
"anomalies": ...,
"chart_plan": ...
}

Do not send unnecessary text.

Required JSON Schema

The model MUST return

{
"executive_summary": "...",

"strengths": [
"...",
"...",
"..."
],

"risks": [
"...",
"...",
"..."
],

"opportunities": [
"...",
"...",
"..."
],

"recommended_questions": [
"...",
"...",
"..."
],

"priority_actions": [
{
"title": "...",
"priority": "High",
"reason": "..."
}
],

"analysis_confidence": 0-100,

"dataset_quality_score": 0-100
}

Return ONLY JSON.

Nothing else.

Validation

Validate the returned JSON.

If parsing fails

Retry once.

If retry also fails

Raise a clean exception.

Do not crash the API.

analyze.py

Modify analyze.py.

Remove

BusinessContextBuilder

Replace it with

LLMBusinessContext

Everything else must remain identical.

No schema changes.

No response changes.

No frontend changes.

Error Handling

If OpenAI is unavailable

If timeout occurs

If rate limited

If invalid JSON

Return a graceful fallback BusinessContext.

The fallback should include

executive summary
strengths
risks
opportunities
recommended questions
priority actions

generated from simple deterministic templates.

Never return null.

Never break the API.

Code Quality

Use

type hints
docstrings
logging
async implementation
small helper functions
environment variables
retry logic
timeout handling

Avoid duplicated code.

Keep the service under roughly 250 lines.

Deliverables

Generate all required code.

Modify imports.

Modify analyze.py.

Create the new service.

Remove BusinessContextBuilder usage.

Preserve the existing API contract completely.

The application should run without requiring frontend changes.